#include <stdio.h> // Inclusion de la bibliothèque standard d'entrée/sortie

int main()
{
    int opération;

    opération = 12 - 5;
    opération = opération *2;

    printf("%d\n", opération);
    return(0);
}
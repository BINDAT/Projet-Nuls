#include <stdio.h>
#include <stdlib.h>

int main ()
{
    printf("Validez par Entree pour vider l'affiche !");
    getchar();
    //system("cls"); /* Windows only */
    system("clear"); /* Mac - Unix */
    puts("That's better");
    return(0);
}
#include <stdio.h>

int main (int argc, char *argv[])//fonction principale avec en paramètres argc et *argv
{
    printf("Cet argument est = %s.\n",argv[0]);//affiche argv
    return(0);
}

/*!Ce code dois être compiler pour être executer dans un terminal! Ce code afficheras tout les argument entrer dans le terminale*/
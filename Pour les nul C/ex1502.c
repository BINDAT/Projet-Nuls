#include <stdio.h>

int main (int argc, char *argv[])
{
    printf("Nombre d'arguments fourni = %d\n",argc);
    return(0);
}

/*
Observation : ce code effectue un affichage sur le nombre d'argument.
!ATTENTION! : Ce code dois être compiler, est ensuite executer dans un terminal.
*/
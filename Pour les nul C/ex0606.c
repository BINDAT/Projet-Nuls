#include <stdio.h> //librairie
#define GLORKUS 16 //définition d'une constante.
int main(){
    int blorf; //variable
    blorf = 22; //valeur de la variable
    printf("La valeur de blorf est %i\n", blorf);
    printf("La valeur de blorf est +16 est %i\n", blorf+GLORKUS);
    printf("La valeur de blorf puissance deux est %i\n", blorf*blorf);
    return(0); 
}
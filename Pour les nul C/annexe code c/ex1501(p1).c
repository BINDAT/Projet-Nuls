#include <stdio.h>

int main (int argc, char *argv[])
{
    printf("Bienvenue, %s\n",argv[0]);//Renvoie en affichage le lien du dossier avec le message de bienvenue quand à 0 sinon renvoie null
    return(0);
}